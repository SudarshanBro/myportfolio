import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { ThemeProvider } from 'next-themes';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Sudarshan Acharya - Full Stack Developer',
  description: 'Portfolio of Sudarshan Acharya - Electronics Engineer & Full Stack Developer specializing in React.js, AI/ML, and Mobile Development',
  keywords: 'Sudarshan Acharya, Full Stack Developer, React.js, AI/ML, Electronics Engineer, Nepal',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange={false}
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}